{
  "version": 2,
  "builds": [
    {
      "src": "./client",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npm run build",
        "outputDirectory": "dist"
      }
    },
    {
      "src": "./server",
      "use": "@vercel/node"
    }
  ]
}
